namespace WillCorp
{
    /// <summary>
    /// This interface is used a marker interface, it has no attributes nor behavior
    ///and is only used to indicate that a implementing class in a plugin, which will be
    ///loaded dynamically
    /// </summary>
    public interface IServicePlugin { }
}
